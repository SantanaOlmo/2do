1. guarda la api key en una variable de entorno en el archivo .env
2. isntala dotenv con `npm install dotenv` (para java) o `pip install python-dotenv`
3. Instala el sdk de ia generativa de google `pip install -q -U google-genai`
4. Crea un archivo 